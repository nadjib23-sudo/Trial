<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>JAWAHER — Horlogerie Fine</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Oswald:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#0a0c10;
    --panel:#12161f;
    --panel-2:#171c27;
    --gold:#c9a24b;
    --gold-dim:#8a6f34;
    --parchment:#ede6d6;
    --slate:#5c6472;
    --line: rgba(201,162,75,0.18);
  }
  *{margin:0;padding:0;box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  body{
    background:var(--ink);
    color:var(--parchment);
    font-family:'Inter',sans-serif;
    overflow-x:hidden;
  }
  ::selection{background:var(--gold);color:var(--ink);}

  .eyebrow{
    font-family:'JetBrains Mono',monospace;
    font-size:11px;
    letter-spacing:3px;
    text-transform:uppercase;
    color:var(--gold);
  }
  h1,h2,h3{
    font-family:'Oswald',sans-serif;
    font-weight:500;
    text-transform:uppercase;
    letter-spacing:1px;
  }

  /* NAV */
  nav{
    position:fixed;top:0;left:0;right:0;z-index:100;
    display:flex;align-items:center;justify-content:space-between;
    padding:22px 5vw;
    background:linear-gradient(to bottom, rgba(10,12,16,0.9), transparent);
    transition:background .3s;
  }
  nav.scrolled{background:rgba(10,12,16,0.92);border-bottom:1px solid var(--line);backdrop-filter:blur(6px);}
  .logo{
    font-family:'Oswald',sans-serif;
    font-size:20px;letter-spacing:5px;font-weight:600;
    color:var(--parchment);
  }
  .logo span{color:var(--gold);}
  .nav-links{display:flex;gap:38px;list-style:none;}
  .nav-links a{
    color:var(--parchment);text-decoration:none;
    font-family:'JetBrains Mono',monospace;
    font-size:11px;letter-spacing:2px;text-transform:uppercase;
    opacity:.75;transition:opacity .25s, color .25s;
    position:relative;
  }
  .nav-links a:hover{opacity:1;color:var(--gold);}
  .nav-cta{
    border:1px solid var(--gold-dim);
    color:var(--gold);
    padding:9px 18px;
    font-family:'JetBrains Mono',monospace;
    font-size:11px;letter-spacing:2px;text-transform:uppercase;
    text-decoration:none;
    transition:background .25s, color .25s;
    white-space:nowrap;
  }
  .nav-cta:hover{background:var(--gold);color:var(--ink);}
  .burger{display:none;}

  /* HERO */
  .hero{
    min-height:100vh;
    display:flex;align-items:center;
    padding:0 5vw;
    position:relative;
    background:
      radial-gradient(ellipse at 75% 40%, rgba(201,162,75,0.10), transparent 55%),
      var(--ink);
  }
  .hero-grid{
    display:grid;grid-template-columns:1fr 1fr;gap:40px;
    width:100%;align-items:center;
  }
  .hero-copy .eyebrow{margin-bottom:18px;display:block;}
  .hero-copy h1{
    font-size:clamp(40px,6vw,76px);
    line-height:0.98;
    margin-bottom:22px;
  }
  .hero-copy h1 em{
    font-style:normal;color:var(--gold);
    display:block;
  }
  .hero-copy p{
    color:var(--slate);
    max-width:420px;
    font-size:15px;line-height:1.7;
    margin-bottom:34px;
  }
  .hero-actions{display:flex;gap:16px;align-items:center;flex-wrap:wrap;}
  .btn-primary{
    background:var(--gold);color:var(--ink);
    padding:15px 30px;
    font-family:'JetBrains Mono',monospace;
    font-size:12px;letter-spacing:2px;text-transform:uppercase;
    text-decoration:none;font-weight:500;
    transition:transform .25s, box-shadow .25s;
    display:inline-block;
  }
  .btn-primary:hover{transform:translateY(-2px);box-shadow:0 10px 30px rgba(201,162,75,0.25);}
  .btn-ghost{
    color:var(--parchment);text-decoration:none;
    font-family:'JetBrains Mono',monospace;
    font-size:12px;letter-spacing:2px;text-transform:uppercase;
    border-bottom:1px solid var(--line);
    padding-bottom:3px;
  }

  /* WATCH VISUAL (CSS drawn) */
  .watch-stage{
    display:flex;align-items:center;justify-content:center;
    position:relative;
  }
  .watch{
    width:min(360px,80vw);height:min(360px,80vw);
    border-radius:50%;
    position:relative;
    background:
      radial-gradient(circle at 35% 30%, #262b36, #0d1015 70%);
    border:10px solid #1c212c;
    box-shadow:
      0 0 0 1px rgba(201,162,75,0.35),
      0 30px 70px rgba(0,0,0,0.6),
      inset 0 0 40px rgba(0,0,0,0.6);
  }
  .watch::before{
    content:'';position:absolute;inset:14px;border-radius:50%;
    border:1px solid var(--line);
  }
  .tick{position:absolute;background:var(--gold);opacity:.8;}
  .tick.h{width:2px;height:14px;top:8px;left:50%;transform-origin:1px 168px;}
  .hand{
    position:absolute;left:50%;top:50%;
    background:var(--parchment);
    transform-origin:bottom center;
    border-radius:3px;
  }
  .hand.hour{width:5px;height:80px;margin-left:-2.5px;margin-top:-80px;background:var(--parchment);}
  .hand.min{width:3px;height:115px;margin-left:-1.5px;margin-top:-115px;background:var(--parchment);}
  .hand.sec{width:1.5px;height:130px;margin-left:-.75px;margin-top:-130px;background:var(--gold);}
  .hub{position:absolute;left:50%;top:50%;width:12px;height:12px;background:var(--gold);border-radius:50%;transform:translate(-50%,-50%);z-index:5;}
  .watch-label{
    position:absolute;top:38%;left:50%;transform:translate(-50%,-50%);
    text-align:center;z-index:3;
  }
  .watch-label .brand{font-family:'Oswald';font-size:12px;letter-spacing:4px;color:var(--parchment);}
  .watch-label .swiss{font-family:'JetBrains Mono';font-size:8px;letter-spacing:2px;color:var(--slate);margin-top:2px;}

  .scroll-cue{
    position:absolute;bottom:36px;left:5vw;
    display:flex;align-items:center;gap:10px;
    font-family:'JetBrains Mono',monospace;font-size:10px;letter-spacing:2px;
    color:var(--slate);text-transform:uppercase;
  }
  .scroll-cue .line{width:32px;height:1px;background:var(--slate);position:relative;overflow:hidden;}
  .scroll-cue .line::after{
    content:'';position:absolute;left:-100%;top:0;width:100%;height:100%;background:var(--gold);
    animation:scrollline 1.8s infinite;
  }
  @keyframes scrollline{to{left:100%;}}

  /* TICKER */
  .ticker-wrap{
    border-top:1px solid var(--line);border-bottom:1px solid var(--line);
    padding:16px 0;overflow:hidden;white-space:nowrap;background:var(--panel);
  }
  .ticker{display:inline-block;animation:ticker 28s linear infinite;}
  .ticker span{
    font-family:'JetBrains Mono',monospace;font-size:12px;letter-spacing:3px;
    color:var(--slate);text-transform:uppercase;margin-right:60px;
  }
  .ticker span em{color:var(--gold);font-style:normal;}
  @keyframes ticker{from{transform:translateX(0);}to{transform:translateX(-50%);}}

  /* SECTION generic */
  section{padding:130px 5vw;position:relative;}
  .section-head{margin-bottom:70px;max-width:640px;}
  .section-head .eyebrow{margin-bottom:14px;display:block;}
  .section-head h2{font-size:clamp(28px,3.6vw,44px);}
  .fade-up{opacity:0;transform:translateY(28px);transition:opacity .8s ease, transform .8s ease;}
  .fade-up.in{opacity:1;transform:translateY(0);}

  /* MECHANISM */
  .mech{
    display:grid;grid-template-columns:0.9fr 1.1fr;gap:60px;align-items:center;
  }
  .mech-visual{
    position:relative;height:420px;
    background:radial-gradient(circle at 50% 50%, rgba(201,162,75,0.08), transparent 60%);
    display:flex;align-items:center;justify-content:center;
  }
  .gear{
    position:absolute;border-radius:50%;
    border:2px solid var(--gold-dim);
    background:repeating-conic-gradient(var(--gold-dim) 0deg 8deg, transparent 8deg 20deg);
    opacity:.9;
  }
  .gear::after{content:'';position:absolute;inset:30%;border-radius:50%;background:var(--ink);border:1px solid var(--gold-dim);}
  .gear.g1{width:210px;height:210px;animation:spin 18s linear infinite;}
  .gear.g2{width:130px;height:130px;top:10%;right:14%;animation:spinRev 12s linear infinite;}
  .gear.g3{width:90px;height:90px;bottom:8%;left:12%;animation:spin 9s linear infinite;}
  @keyframes spin{to{transform:rotate(360deg);}}
  @keyframes spinRev{to{transform:rotate(-360deg);}}

  .spec-list{border-top:1px solid var(--line);}
  .spec-row{
    display:flex;justify-content:space-between;align-items:baseline;
    padding:18px 0;border-bottom:1px solid var(--line);
  }
  .spec-row .k{
    font-family:'JetBrains Mono',monospace;font-size:11px;letter-spacing:2px;
    color:var(--slate);text-transform:uppercase;
  }
  .spec-row .v{font-family:'Oswald';font-size:18px;color:var(--gold);letter-spacing:.5px;}
  .freq-highlight{font-size:64px;color:var(--gold);font-family:'Oswald';line-height:1;margin:8px 0 24px;}
  .freq-note{color:var(--slate);font-size:13px;max-width:360px;line-height:1.7;margin-bottom:30px;}

  /* COLLECTION */
  .collection-grid{
    display:grid;grid-template-columns:repeat(3,1fr);gap:1px;
    background:var(--line);border:1px solid var(--line);
  }
  .product-card{
    background:var(--ink);padding:44px 30px 34px;
    display:flex;flex-direction:column;align-items:center;text-align:center;
    transition:background .3s;
  }
  .product-card:hover{background:var(--panel);}
  .mini-watch{
    width:130px;height:130px;border-radius:50%;margin-bottom:26px;
    background:radial-gradient(circle at 35% 30%, #262b36, #0d1015 70%);
    border:6px solid #1c212c;
    box-shadow:0 0 0 1px rgba(201,162,75,0.3), 0 18px 40px rgba(0,0,0,0.5);
    position:relative;
  }
  .mini-watch::before{content:'';position:absolute;inset:10px;border-radius:50%;border:1px solid var(--line);}
  .mini-watch .hand{background:var(--parchment);}
  .mini-watch .hand.hour{width:3px;height:28px;margin-left:-1.5px;margin-top:-28px;}
  .mini-watch .hand.min{width:2px;height:42px;margin-left:-1px;margin-top:-42px;}
  .product-card.noir .mini-watch{border-color:#0a0a0a;}
  .product-card.argent .mini-watch::before{border-color:rgba(201,162,75,0.5);}
  .product-card.or .mini-watch{box-shadow:0 0 0 1px rgba(201,162,75,0.6), 0 18px 40px rgba(0,0,0,0.5);}
  .p-ref{font-family:'JetBrains Mono';font-size:10px;letter-spacing:2px;color:var(--slate);margin-bottom:8px;}
  .p-name{font-family:'Oswald';font-size:20px;letter-spacing:1px;margin-bottom:10px;}
  .p-desc{color:var(--slate);font-size:13px;line-height:1.6;margin-bottom:18px;min-height:40px;}
  .p-price{font-family:'JetBrains Mono';color:var(--gold);font-size:15px;margin-bottom:22px;letter-spacing:1px;}
  .p-cta{
    border:1px solid var(--gold-dim);color:var(--gold);
    padding:11px 22px;font-family:'JetBrains Mono';font-size:10px;
    letter-spacing:2px;text-transform:uppercase;text-decoration:none;
    transition:background .25s,color .25s;
  }
  .p-cta:hover{background:var(--gold);color:var(--ink);}

  /* TRUST STRIP */
  .trust{
    display:grid;grid-template-columns:repeat(4,1fr);
    border-top:1px solid var(--line);border-bottom:1px solid var(--line);
  }
  .trust-item{padding:34px 24px;border-right:1px solid var(--line);}
  .trust-item:last-child{border-right:none;}
  .trust-item .num{font-family:'Oswald';color:var(--gold);font-size:28px;margin-bottom:6px;}
  .trust-item .lbl{font-family:'JetBrains Mono';font-size:10px;letter-spacing:2px;color:var(--slate);text-transform:uppercase;}

  /* CONTACT / FOOTER */
  .contact{
    text-align:center;padding-top:110px;padding-bottom:60px;
  }
  .contact h2{font-size:clamp(30px,5vw,58px);max-width:760px;margin:0 auto 34px;}
  .contact-actions{display:flex;gap:18px;justify-content:center;flex-wrap:wrap;margin-bottom:20px;}
  .whatsapp-btn{
    background:#25D366;color:#062b14;
    padding:16px 34px;border-radius:2px;
    font-family:'JetBrains Mono';font-size:12px;letter-spacing:2px;text-transform:uppercase;
    text-decoration:none;font-weight:600;
    display:inline-flex;align-items:center;gap:10px;
    transition:transform .25s;
  }
  .whatsapp-btn:hover{transform:translateY(-2px);}
  footer{
    border-top:1px solid var(--line);
    padding:34px 5vw;display:flex;justify-content:space-between;align-items:center;
    flex-wrap:wrap;gap:16px;
  }
  footer .logo{font-size:14px;}
  .foot-links{display:flex;gap:26px;list-style:none;}
  .foot-links a{color:var(--slate);text-decoration:none;font-family:'JetBrains Mono';font-size:11px;letter-spacing:1px;transition:color .2s;}
  .foot-links a:hover{color:var(--gold);}
  .foot-copy{color:var(--slate);font-family:'JetBrains Mono';font-size:10px;letter-spacing:1px;}

  @media(max-width:860px){
    .nav-links{display:none;}
    .hero-grid{grid-template-columns:1fr;text-align:center;}
    .hero-copy p{margin-left:auto;margin-right:auto;}
    .hero-actions{justify-content:center;}
    .watch-stage{order:-1;margin-bottom:20px;}
    .mech{grid-template-columns:1fr;}
    .mech-visual{height:280px;}
    .collection-grid{grid-template-columns:1fr;}
    .trust{grid-template-columns:1fr 1fr;}
    .trust-item{border-bottom:1px solid var(--line);}
    footer{flex-direction:column;text-align:center;}
  }
  @media (prefers-reduced-motion: reduce){
    *{animation-duration:0.01ms !important; transition-duration:0.01ms !important;}
  }
</style>
</head>
<body>

<nav id="nav">
  <div class="logo">JAWAHER<span>.</span></div>
  <ul class="nav-links">
    <li><a href="#collection">Collection</a></li>
    <li><a href="#mecanisme">Mécanisme</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
  <a class="nav-cta" href="#contact">Commander</a>
</nav>

<section class="hero">
  <div class="hero-grid">
    <div class="hero-copy fade-up in">
      <span class="eyebrow">Jawaher — Horlogerie Algérienne</span>
      <h1>Équilibre<em>Métallique</em></h1>
      <p>Des montres pensées comme des instruments de précision — boîtier en acier inoxydable, mouvement à quartz haute stabilité, gravures faites à la main. Conçues pour durer, portées pour être remarquées.</p>
      <div class="hero-actions">
        <a href="#collection" class="btn-primary">Voir la collection</a>
        <a href="#mecanisme" class="btn-ghost">Découvrir le mécanisme</a>
      </div>
    </div>
    <div class="watch-stage">
      <div class="watch" id="heroWatch">
        <div class="watch-label"><div class="brand">JAWAHER</div><div class="swiss">TYPE — 01</div></div>
        <div class="hand hour" id="hHour"></div>
        <div class="hand min" id="hMin"></div>
        <div class="hand sec" id="hSec"></div>
        <div class="hub"></div>
      </div>
    </div>
  </div>
  <div class="scroll-cue"><span>Défiler</span><span class="line"></span></div>
</section>

<div class="ticker-wrap">
  <div class="ticker">
    <span><em>✓</em> Livraison 58 wilayas</span>
    <span><em>✓</em> Paiement à la livraison</span>
    <span><em>✓</em> Garantie 12 mois</span>
    <span><em>✓</em> Gravure personnalisée offerte</span>
    <span><em>✓</em> Livraison 58 wilayas</span>
    <span><em>✓</em> Paiement à la livraison</span>
    <span><em>✓</em> Garantie 12 mois</span>
    <span><em>✓</em> Gravure personnalisée offerte</span>
  </div>
</div>

<section id="mecanisme">
  <div class="section-head fade-up">
    <span class="eyebrow">Composants // 02. Mouvement</span>
    <h2>Précision à chaque rotation</h2>
  </div>
  <div class="mech">
    <div class="mech-visual fade-up">
      <div class="gear g1"></div>
      <div class="gear g2"></div>
      <div class="gear g3"></div>
    </div>
    <div class="fade-up">
      <div class="freq-highlight">4 HZ</div>
      <p class="freq-note">Le mouvement à quartz oscille à 32 768 vibrations par seconde, régulé pour une précision constante — quelle que soit la wilaya, quelle que soit la saison.</p>
      <div class="spec-list">
        <div class="spec-row"><span class="k">Fréquence</span><span class="v">4 Hz</span></div>
        <div class="spec-row"><span class="k">Étanchéité</span><span class="v">5 ATM</span></div>
        <div class="spec-row"><span class="k">Boîtier</span><span class="v">Acier 316L</span></div>
        <div class="spec-row"><span class="k">Verre</span><span class="v">Saphir minéral</span></div>
        <div class="spec-row"><span class="k">Garantie</span><span class="v">12 mois</span></div>
      </div>
    </div>
  </div>
</section>

<section id="collection" style="padding-top:40px;">
  <div class="section-head fade-up">
    <span class="eyebrow">Collection // IK Series</span>
    <h2>Trois interprétations, un seul standard</h2>
  </div>
  <div class="collection-grid fade-up">
    <div class="product-card noir">
      <div class="mini-watch"><div class="hand hour"></div><div class="hand min"></div></div>
      <div class="p-ref">RÉF. IK—01</div>
      <div class="p-name">Noir Gravé</div>
      <div class="p-desc">Boîtier noir mat, cadran squelette, bracelet acier gravé sur demande.</div>
      <div class="p-price">8 900 DA</div>
      <a class="p-cta" href="#contact">Commander</a>
    </div>
    <div class="product-card argent">
      <div class="mini-watch"><div class="hand hour"></div><div class="hand min"></div></div>
      <div class="p-ref">RÉF. IK—02</div>
      <div class="p-name">Argent Classique</div>
      <div class="p-desc">Finition argentée intemporelle, index appliqués, fond saphir transparent.</div>
      <div class="p-price">9 500 DA</div>
      <a class="p-cta" href="#contact">Commander</a>
    </div>
    <div class="product-card or">
      <div class="mini-watch"><div class="hand hour"></div><div class="hand min"></div></div>
      <div class="p-ref">RÉF. IK—03</div>
      <div class="p-name">Or Édition</div>
      <div class="p-desc">Plaqué or 18K, édition limitée, écrin signature inclus.</div>
      <div class="p-price">11 900 DA</div>
      <a class="p-cta" href="#contact">Commander</a>
    </div>
  </div>
</section>

<div class="trust">
  <div class="trust-item"><div class="num">+2400</div><div class="lbl">Clients livrés</div></div>
  <div class="trust-item"><div class="num">58</div><div class="lbl">Wilayas couvertes</div></div>
  <div class="trust-item"><div class="num">12</div><div class="lbl">Mois de garantie</div></div>
  <div class="trust-item"><div class="num">4.8/5</div><div class="lbl">Note moyenne</div></div>
</div>

<section id="contact" class="contact">
  <span class="eyebrow">Passer commande</span>
  <h2 style="margin-top:18px;">Choisissez votre modèle, on s'occupe du reste — livraison à domicile, paiement à la réception.</h2>
  <div class="contact-actions">
    <a class="whatsapp-btn" href="https://wa.me/213000000000" target="_blank" rel="noopener">
      ● Commander sur WhatsApp
    </a>
    <a class="btn-ghost" href="#" style="align-self:center;">Voir sur Facebook</a>
  </div>
</section>

<footer>
  <div class="logo">JAWAHER<span>.</span></div>
  <ul class="foot-links">
    <li><a href="#collection">Collection</a></li>
    <li><a href="#mecanisme">Mécanisme</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
  <div class="foot-copy">© 2026 JAWAHER SHOP — ALGÉRIE</div>
</footer>

<script>
  // Nav background on scroll
  const nav = document.getElementById('nav');
  window.addEventListener('scroll', () => {
    nav.classList.toggle('scrolled', window.scrollY > 40);
  });

  // Live watch hands
  function tickClock(){
    const now = new Date();
    const s = now.getSeconds() + now.getMilliseconds()/1000;
    const m = now.getMinutes() + s/60;
    const h = (now.getHours()%12) + m/60;
    const hourEl = document.getElementById('hHour');
    const minEl = document.getElementById('hMin');
    const secEl = document.getElementById('hSec');
    if(hourEl) hourEl.style.transform = `rotate(${h*30}deg)`;
    if(minEl) minEl.style.transform = `rotate(${m*6}deg)`;
    if(secEl) secEl.style.transform = `rotate(${s*6}deg)`;
    requestAnimationFrame(tickClock);
  }
  tickClock();

  // static hands for mini watches (decorative fixed positions)
  document.querySelectorAll('.mini-watch').forEach((w,i)=>{
    const hour = w.querySelector('.hand.hour');
    const min = w.querySelector('.hand.min');
    if(hour) hour.style.transform = `rotate(${40+i*70}deg)`;
    if(min) min.style.transform = `rotate(${160+i*50}deg)`;
  });

  // Scroll reveal
  const observer = new IntersectionObserver((entries)=>{
    entries.forEach(e=>{
      if(e.isIntersecting){ e.target.classList.add('in'); }
    });
  }, {threshold:0.15});
  document.querySelectorAll('.fade-up').forEach(el=>observer.observe(el));
</script>

</body>
</html>
